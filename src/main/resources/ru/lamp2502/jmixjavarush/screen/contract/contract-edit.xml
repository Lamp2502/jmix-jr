<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://jmix.io/schema/ui/window"
        caption="msg://contractEdit.caption"
        focusComponent="form">
    <data>
        <instance id="contractDc"
                  class="ru.lamp2502.jmixjavarush.entity.document.Contract">
            <fetchPlan extends="_base"/>
            <loader/>
        </instance>
    </data>
    <facets>
        <dataLoadCoordinator auto="true"/>
        <screenSettings id="settingsFacet" auto="true"/>
    </facets>
    <actions>
        <action id="windowCommitAndClose" caption="msg:///actions.Ok"
                icon="EDITOR_OK"
                primary="true"
                shortcut="${COMMIT_SHORTCUT}"/>
        <action id="windowClose"
                caption="msg:///actions.Close"
                icon="EDITOR_CANCEL"/>
    </actions>
    <dialogMode height="600"
                width="800"/>
    <layout spacing="true" expand="scrollBox">
        <scrollBox id="scrollBox" spacing="true">
            <form id="form" dataContainer="contractDc">
                <column width="350px">
                    <textField id="titelField" property="titel"/>
                    <textArea id="bodyField" property="body" rows="5"/>
                    <comboBox id="customerField" property="customer"/>
                    <comboBox id="performerField" property="performer"/>
                    <textField id="numberField" property="number"/>
                    <comboBox id="statusField" property="status"/>
                    <comboBox id="communicationTypeField" property="communicationType"/>
                </column>
            </form>
        </scrollBox>
        <hbox id="editActions" spacing="true">
            <button id="commitAndCloseBtn" action="windowCommitAndClose"/>
            <button id="closeBtn" action="windowClose"/>
        </hbox>
    </layout>
</window>
